"""
Este file usualmente lo pondría en .gitignore, pero a los propósito de la demo, va a quedar visible en el repo.
"""
AZURE_SQL_CONNECTIONSTRING = <db-connection-string>
TOKEN_URL = <azure-token-url>
SQL_COPT_SS_ACCESS_TOKEN = <token>  #pre-connection attribute allows the use of an access token obtained from Microsoft Entra ID for authentication instead of username and password, and also bypasses the negotiation and obtaining of an access token by the driver. 
target_table_name = <target-table-name>
container_name = <container-name>
blob_name = <blob-name>